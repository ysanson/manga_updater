pub struct MangaChapter {
    pub manga_title: String,
    pub url: String,
    pub chapter_title: String,
    pub num: f32
}

pub struct CSVLine {
    pub url: String,
    pub last_chapter_num: f32
}